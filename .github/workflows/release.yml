name: Build & Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: <owner>/ps-release-workflow/.github/workflows/release.yml@main
    secrets:
      PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
      LICENSE_KEY: ${{ secrets.LICENSE_KEY }}